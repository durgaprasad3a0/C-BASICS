#include<stdio.h>
void main()
{
	int number;
	printf("enter the number");
	scanf("%d",&number);
	if(number>0)
	printf("%d is postivie number",number);
	else if(number=0)
	printf("&d is a integer number",number);
	else
	printf("%d is negative number",number);
}
